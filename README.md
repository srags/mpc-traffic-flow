# Quantifying Controllable Highway Congestion

This repo contains the code, data and results for paper [Toward Controllability-Aware Performance Measures: A Case Study on Controllable Highway Congestion](https://arxiv.org/abs/2608.18510v1). This paper is currently under review at Transportation Research: Part C.

The pipeline has three stages:

1. **Calibrate** a METANET second-order traffic model to a day of field data.
2. **Optimize** a VSL policy against the calibrated model with a nonlinear MPC (using the Pyomo package with IPOPT solver).
3. **Analyze** the gap between the uncontrolled and optimally-controlled simulations. 

Controllable Congestion is defined as

```
delay      = TTS_uncontrolled - TTS_freeflow
delay_opt  = TTS_controlled   - TTS_freeflow
CC (%)     = (delay - delay_opt) / delay * 100
```

where TTS is total time spent (veh-hr). See `run_one_day` in [src/cc_analysis.py](src/cc_analysis.py) for the reference implementation.

**Case studies considered.**  (a) A synthetic 10 km bottleneck (4 lanes dropping to 2) swept over peak demand. (b) Ten weekdays of I-24 MOTION data (Nov 21 – Dec 2, 2022), a 5.6 km corridor of 14 segments at 400 m / 10 s resolution.

---

## 1. Quick start

```bash
conda env create -f environment.yml
conda activate automatic_control_2
```

IPOPT is **not** installed by conda. It should be installed using homebrew for macOS:

```bash
brew install ipopt
```

To ensure that all the paths are correct, run:
```bash
python src/paths.py
```
It should say OK for all paths and point to the appropriate folders on your local machine.

To check that the environment is working, run the following command:

```bash
python experiments/I_24_tsd_plotting.py
```

It should plot Figure 6 to `figs/i24_tsd.png`. It needs no path edits and works from any directory.

---

## 2. Repository layout

| Path | What it is |
|---|---|
| `src/` | Main source code, experiments import functions from here. |
| `experiments/` | `.py` scripts regenerate paper figures; `.ipynb` notebooks are where new controllable congestion runs are launched. |
| `data/` | Input field data **and** the calibrated model parameters. Read-only in practice — see [§4](#4-data). |
| `results/` | Saved VSL solutions (`optimal_vsl.npy` + a `_config.json` for each).|
| `figs/` | Paper figures. Generated by the scripts in `experiments/`. |
| `experiments/old/` | Superseded scripts and exploratory notebooks. Kept for reference; expect stale paths. |

### `src/` modules

| Module | Role |
|---|---|
| `traffic_sim.py`* | METANET forward simulator. `run_metanet_sim` is the main function here. |
| `mpc_metanet.py`* | Code for the MPC process. `mpc_find_vsl` runs the rolling horizon loop; `mpc_opt` builds and solves one Pyomo NLP. |
| `optimization_utils.py` | Model calibration. `run_calibration` fits METANET parameters to `(rho_hat, q_hat)`. |
| `paths.py` | Every path in the repo, derived from the repo root. Import from here rather than writing a path literal — see [§3](#3-paths-and-portability). |
| `param_loader.py`* | `METANET_Params` class loads a calibration directory into the parameter dict every other function expects. Handles both static and time-varying (`control_h_*`) calibrations. |
| `cc_analysis.py` | This file does I-24 analysis layer after other `optimal_vsl.npy` files have been generated.|
| `eval_metrics.py` | Contains evaluation metrics like MAPE/RMSE and calibration robustness-to-noise. |
| `generate_demand_synthetic.py` | Synthetic bottleneck demand profiles and the demand sweep. |
| `generate_vt.py` | Virtual trajectories through a speed field (travel-time stats). |
| `plotting.py` | Generic VSL / time-space plotting helpers. |
| `warm_start.py` | Warm-start policy libraries for the synthetic constraint sweeps. |

 (* are most important for running controllable congestion)

---



<!-- ## 3. Paths and portability

**There are no machine-specific paths left in `src/` or `experiments/`.** A fresh
clone runs as-is; nobody needs to edit a path to collaborate on this repo. If you
are about to hardcode an absolute path, read this section instead.

### 3.1 How it works

[src/paths.py](src/paths.py) is the single source of truth. It derives the repo
root from its own `__file__`, so it is correct on any machine, under any clone
name, from any working directory:

```python
REPO_ROOT = <the parent of src/>
DATA_ROOT, RESULTS_ROOT, FIGS_ROOT, SRC_ROOT
I24_DATA, I24_RESULTS, SYNTHETIC_RESULTS
DEFAULT_CALIBRATION = "calibration_static/fixed_ramping"
```

with helpers that build the paths this project actually uses:

| Call | Returns |
|---|---|
| `i24_data("11_30")` | `<data>/i24/i24_11_30` |
| `i24_data("11_30", "q_hat.npy")` | the flow file for that date |
| `i24_calibration("11_30")` | that date's static calibration directory |
| `i24_calibration("11_30", "calibration_dynamic", 90)` | `.../calibration_dynamic/control_h_90` |
| `i24_results("11_30")` | `<results>/i24/i24_11_30/calibration_static/fixed_ramping` |
| `i24_results("11_30", "speed_lb")` | the min-speed-limit sweep directory |
| `synthetic_results("demand")` | `<results>/synthetic_10km/demand` |
| `fig("i24_tsd.png")` | a path under `figs/`, creating the directory if needed |
| `ensure_dir(p)` | `p`, created if missing — safe to wrap any output path |
| `i24_dates()` | every date directory actually present |

Everything returns `str`, never `pathlib.Path`, so these drop into the existing
f-string and `+` path building without a single call site needing to change style.

**Diagnostic.** When something cannot find its data, run this first:

```bash
python src/paths.py
```

It prints every resolved root, whether it exists, which environment overrides are
active, and which dates are available. -->
<!-- 
### 3.2 When the data is not inside the repo -->


## 3. Data

### 3.1 I-24 MOTION (`data/i24/`)

One directory per date, `i24_MM_DD`, for ten weekdays: 11_21 – 11_25, 11_28 – 12_02. (11/24 and 11/25 are Thanksgiving and the day after; `cc_analysis.format_date_label` tags them "Holiday" rather than a weekday name.)

Each date directory contains a 1-hour window at 10 s resolution:

| File | Shape | Meaning |
|---|---|---|
| `q_hat.npy` | (360, 16) | Flow, veh/hr. |
| `rho_hat.npy` | (360, 16) | Density, veh/km. |
| `v_hat.npy` | (360, 16) | Speed, km/hr. |
| `lane_mapping.npy` | (16,) | Lanes per segment. |
| `on_ramp_mapping.npy`, `off_ramp_mapping.npy` | (16,) bool | Ramp presence per segment. |

**The 16 columns are 14 modeled segments plus two boundary cells.** Column 0 is the upstream boundary (supplies inflow), column 15 is the downstream boundary (supplies downstream density). All data loading slices `[:, 1:-1]`. 14 segments × 400 m = 5.6 km. Velocity is *derived* as `q_hat / rho_hat` rather than read from `v_hat.npy` — zeros are floored to `1e-3` first to avoid dividing by zero.

### 3.2 Calibrated parameters (`data/i24/<date>/calibration_*/`)

Calibration **output** is saved next to the input data.

- `calibration_static/fixed_ramping` — one parameter set for the whole hour. **This is what every paper figure uses.**
- `calibration_static/time_varying_ramping` — ramp terms allowed to vary. Not used in the paper.
- `calibration_dynamic/` — parameters re-fit every `control_h` steps, stored as `control_h_<N>/params_<i>/`. Only fitted for 11_30.

Each holds `tau.npy`, `K.npy`, `eta_high.npy`, `rho_crit.npy`, `v_free.npy`, `a.npy`, `num_lanes.npy`, `beta_array.npy`, `r_inflow_array.npy` (each shape (14,)), plus the model's own predictions (`v_pred`, `rho_pred`, `q_pred`, `v_sim`) for diagnostics. Parameters are loaded through `METANET_Params` in `src/param_loader.py`.

<!-- ### 4.3 Synthetic (`data/synthetic_10km/`)

**Empty, and that is expected.** The synthetic scenario is generated in code, not
loaded: see the constants at the top of
[experiments/synthetic_demand_tsd.py](experiments/synthetic_demand_tsd.py) (25
segments × 400 m, 4 lanes dropping to 2 at 8 km, 2-hour horizon). Only the *solved*
policies are on disk, under `results/synthetic_10km/`. -->
---

## 4. Running the pipeline

### 4.1 Stage 1 — Calibration

**Notebook:** [experiments/metanet_calibration.ipynb](experiments/metanet_calibration.ipynb)

Set `root_path` (cell 3) to the date you want. `run_calibration` (cell 11) solves one large Pyomo NLP fitting `tau, K, eta_high, rho_crit, v_free, a` per segment, plus ramp terms when `include_ramping=True`. Bounds on those parameters are at [src/optimization_utils.py:27-33](src/optimization_utils.py#L27).

### 4.2 Stage 2 — VSL optimization

**Notebook:** [experiments/run_controllable_congestion.ipynb](experiments/run_controllable_congestion.ipynb) (one-off runs and constraint sweeps) and [experiments/run_safety_sweep.ipynb](experiments/run_safety_sweep.ipynb) (the 2-D smoothness grid).

Both follow the same layout: cells 0–8 load data and calibration, cells 9–17 run the uncontrolled baseline, and the solve cell calls `mpc_find_vsl`. The MPC settings used throughout:

```python
pred_horizon    = 45     # time steps (7.5 min)
control_horizon = 5      # time steps (50 s)
hold_len        = 1
v_fd_penalty    = 1e14
control_zone    = list(range(2, num_segments))   # segments 0-1 stay uncontrolled
control_one_segment = None
```

The other parameters the paper sweeps, all passed through `vsl_opt_config`:

| Key | Meaning |
|---|---|
| `speed_lb` | Minimum posted speed limit, km/hr. |
| `hold_length` | Steps a speed limit must be held before it may change. |
| `safety_temporal` | Max VSL change between consecutive 10 s steps, km/hr. |
| `safety_spatial` | Max VSL difference between adjacent segments, km/hr. |

Each run saves `optimal_vsl_<id>.npy` **and** `optimal_vsl_<id>_config.json` beside it.

The MPC is a nonconvex NLP and IPOPT does **not** reliably converge cold on this problem. `mpc_find_vsl` tries warm starts in a fixed order and falls back:

1. `initialize_vsl` if given (usually a neighboring sweep point's solution),
2. the previous MPC step's solution, shifted forward by `control_horizon`,
3. each constant in `init_fixed` in turn (e.g. `[speed_lb, 100, 120, 140, 150]`),
4. fully cold.


### 4.3 Stage 3 — Analysis and figures

All read the saved VSL policies and run them to determine the controllable congestion %.

| Command | Produces |
|---|---|
| `python experiments/I_24_table_plotting.py` | `figs/i24_cc_table.png`, `.tex`, `figs/i24_avg_delay_bar.png` which summarizes statistics across all 10 days of I-24. |
| `python experiments/I_24_tsd_plotting.py` | `figs/i24_tsd.png` plot time-space diagrams, 11/28 vs 12/02. |
| `python experiments/I_24_constraint_plotting.py` | `figs/i24_constraints.png` (2×2 constraint panel), `figs/i24_safety_heatmap.png`. |
| `python experiments/synthetic_demand_tsd.py` | `figs/synthetic_demand_tsd.png`. |
| `python experiments/synthetic_cc_demand.py` | `figs/synthetic_cc_demand.png` — CC vs peak demand. |

---

## 5. Results layout

```
results/i24/i24_<MM_DD>/<calibration_id>/
├── optimal_vsl.npy                    # unconstrained optimum — the CC headline number
├── optimal_vsl_config.json
├── speed_lb/     optimal_vsl_<lb>.npy                       # min speed limit sweep
├── hold_length/  optimal_vsl_<len>.npy                      # hold length sweep
└── safety_sweep/ optimal_vsl_temp<T>_spat<S>.npy            # 2-D smoothness grid

results/synthetic_10km/
├── demand/             demand<peak>_duration<d>.csv
├── min_speedlimit/, holdlength/, holdlength_shifted/
├── spatial_safety/, temporal_safety/    safety<value>.csv
└── gantry/             location<i>.csv                      # single-gantry placement
```

---
